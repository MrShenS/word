day01

1.kerne(内核)
管理设备、内存、程序等等。 
控制系统程序/工具和硬件之间的功能。 
管理交换空间（虚拟页面即虚拟内存）、守护进程、文件系统以及其他功能。 

2.Shell 
Shell是用户和操作系统内核进行通讯的桥梁，扮演着解释器或翻译器的角色。 

3.FileSystem(文件系统) 
具有一定层次结构的目录，子目录和文件组合在一起所形成的系统。
 “/” 根目录，是 Linux/Unix 文件系统的最顶级的目录。 
	eg: /a/b/c   /用于区分每一个目录名标志。
		第一个 / 代表根目录
    绝对路径：absolute pathname   所有以/(根目录)开头的都是绝对路径
		eg.cd /home/briup/abc
    相对路径:relative pathname   没有/的都是相对路径
		eg.cd abc
4.Linux文件系统结构：
“/”  根路径
“/opt”  【Optional(可选的) Application Software Packages】存放第三方工具或者是应用程序的目录。
“/etc”  【Etcetra Directory 】存放和系统相关的配置文件的目录。 
“/dev”  【Devices】存放设备文件的目录。网卡例外，没有设备文件。 
“/usr”  【Unix System Resource】Unix 系统资源，存放系统中所有用户都会使用的命令，文档以及开发库。
		/usr/include: linux下开发和编译引用程序所需要的头文件。
		/usr/local: 本地增加的命令和库。
		/usr/sbin: 超级用户的一些管理程序
		/usr/src：源代码
		/usr/bin: 众多的而应用程序，二进制可执行程序
		/usr/share:共享文件
		/usr/lib:常用的动态链接库和软件包的配置文件。	
“/boot”  存放内核程序(unix叫kernel),系统启动文件，包括Grub,lilo启动器程序。 
“/kernel” 在 Linux 中是“/boot”，存放内核程序。 
“/var”  【Variable(可变的)】存放的是管理员所使用的一些与系统运行过程相关特殊功能。比如操作系统的日志等等。 
“/home” 系统默认的用户的主目录,root 用户(权限最高)除外。
“/bin”: 存放系统中最常用的可执行文件
“/lib”：存放共享的库文件，包含许多被/bin和/sbin中程序使用的库文件
“/lost+found”:这个目录一般情况下是空的，当系统非法关机后，这里就存放了一些零散文件
“/media:ubuntu”系统自动挂载的光驱、usb设备，存放临时读入的文件
“/mnt”：作为被挂载的的文件系统得挂载点。
“/proc”:这个目录是一个虚拟的目录，它是系统内存的映射，我们可以通过直接访问这个目录来获取系统信息，这里存放所有标志位文件的进程。
“/root”:该目录为系统管理员，也称作超级权限者的用户主目录。
“/sbin”:s就是Super User的意思，这里存放的是系统管理员使用的系统管理程序，如系统管理，目录查询等关键命令文件。
		
“/srv”:存放系统所提供的服务数据
“/sys”:系统设备和文件层次结构，并向用户程序提供详细的内核数据信息。
“/tmp”:这个目录用来存放一些临时的文件，所有用户对此目录都有读写权限。



 Command Line Syntax(命令行语法) 


  Command [option[s]] [argument[s]]    大多数的命令选项都会加——
命令行中的每一条命令的命令、选项和参数之间至少以一个空格作为分隔符。 
命令行中的每一条命令最长不能超过265 个字符。 
大小写敏感 

day02


登入/登出命令——login/logout 
 本地登入/登出：能够自己操作，在系统启动后，输入用户名和密码进行登入，使用logout 登出。 
 远程登入 
		SSH远程登录 
		SSH为 Secure Shell的缩写，由 IETF 的网络小组（Network Working Group）所制定，SSH
为建立在应用层基础上的安全协议。 
			登录方式：ssh 用户名@IP，回车之后需要输入密码。 
   		Telnet 远程登录  在登录过程中会以明文的方式发送给主机
		Telnet 协议是 TCP/IP 协议族中的一员，是Internet 远程登陆服务的标准协议和主要方式。
			登录方式：telnet IP，回车之后要求输入用户名，再回车要求出入密码
 远程登出 
   exit 
    如果在当前的 Shell中只有一个用户登录，则该命令会退出控制台。 
    当在同一个 Shell 中有多个用户登录，该命令会逐层退出用户，最后一个用户会退出 Shell。
    该命令不会正常注销用户，只是退出将当前用户退出Shell。 
   logout 
    该命令会正常注销当前用户并且将当前用户退出Shell。 
     在 Shell中登录，切换 Shell之后，logout 会失效，即使切换回之前的Shell，也是失效的。
     
在 Linux 中和用户相关的文件主要是以下三个： 
   “/etc/passwd”,存放用户信息的文件，其中的每一行给出了用户名、密码和用户的其他信息。 
    该目录存储的是操作系统用户信息，该文件为所有用户可见。 
   打开/etc/passwd，看到其中一行的信息如下“kevin:x:1000:1000:Kevin:/home/kevin:/bin/bash”
	可以看出/etc/passwd 文件存放的是用户的信息,由6 个分号组成的 7 个信息,解释如下 
     (1)：用户名。 (2)：密码(已经加密) (3)：UID(用户标识)，操作系统自己用的 
     (4)：GID 组标识。 (5)：用户全名或本地帐号 (6)：家目录 
     (7)：登录使用的 Shell,就是对登录命令进行解析的工具。 
    “/etc/shadow”，存放用户密码信息的文件，是passwd 的影子文件。 
为了安全，现在的 linux 都提供了/etc/shadow 这个影子文件，密码放在这个文件里面，并且是只有root 可读的。 
    在利用了 shadow 文件的情况下，密码用一个 x 表示，普通用户看不到任何密码信息。
  /etc/shadow 文件每一行的格式如下：用户名：加密口令：上一次修改的时间（从1970 年1月
1 日起的天数）：口令在两次修改间的最小天数：口令修改之前向用户发出警告的天数：口令终止后账号被禁
用的天数：从 1970 年 1 月 1 日起账号被禁用的天数：保留域。 
    例如“test:$6$hKjqUA40$OelB9h3UKOgnttKgmRpFr/:14316:0:99999:7:::”，共有 9 个栏目 
     (1)：帐号名称； 
     (2)：密码；这里是加密过的，也可以解密的，要注意安全问题。 
     (3)：上次修改密码的日期（距离1970-01-01 的天数）； 
     (4)：密码不可被变更的天数； 
     (5)：密码需要被重新变更的天数(99999 表示不需要变更)； 
     (6)：密码变更前提前几天警告； 
     (7)：帐号失效日期。 
     (8)：帐号取消日期 
     (9)：保留条目,目前没用 
  “/etc/group”，存放用户组信息的文件，存储有关本地用户组的信息，每一行有一下四部分组成 
    1.GroupName GID 到名称的一种映射，组名 
    2.Password 保存密码的位置 
    3.GID 组 ID 
    4.Users 组成员 

普通用户与超级用户如何进行相互切换
     Sudo su 切换到超级用户
	   exit 退出超级用户 
     su – usename 用户之间切换
修改密码：passwd
密码更改方式：使用 passwd 命令修改密码。 
     1).输入 passwd 用户名回车 
     2).输入当前正在使用的密码 
     3).输入新的密码 
     4).再次输入新的密码 
     5).提示信息，如果提示密码更新成功，则表示密码更改成功。
限制：一般要求应该在六位以上字符，要求是字母和其他字符的混合密码，必须和上次登录密码不同，密码不能和用户名相同。

         终端
Linux   Terminal
Unix     Console
Windows   CMD


基本命令：


ctrl+ alt+t:开启一个终端
ctrl+shift+ "+"：放大
Ctrl+ "-"：放小
vi.xxx:查看并更改某文件
shift+":"+q+!+回车：强制退出文件夹并不更改文件夹内内容
shift+":"+wq+回车:保存文件夹并退出
esc:进入文件夹后如果是插入模式按esc退出插入模式，再退出文件夹


1.clear ：清屏命令
3.pwd：显示当前工作目录的绝对路径名  绝对路径以"/"开头
4.cd:    切换目录
	cd  pathname
	. ： 当前路径
	.. : 父路径(上级目录)
	~ ： 家目录
	cd ~/Desktop ：进入家目录下的Desktop目录
	cd   : 默认回到当前用户的家目录
	cd ~ : 回到当前登录用户的家目录
	cd ~用户名:进入到指定用户的家目录
ps:cd 后面可以跟多个目录但只执行第一个目录 eg.cd /home /opt

4.ls 列出目录下的目录和文件 
  ls -a 列出改目录下的所有目录和文件，其中包含一“.”开头的隐藏文件 
  ls -R 递归显示某个命令下的所有目录和文件
  ls_r:递归显示某个命令下的所有目录和文件
  ls -t 将结果以时间顺序显示 
  ls_lt:同上+详细信息
  ls -l 显示某目录下的目录和文件的详细信息
  ls_la：查看所有文件的长列表信息
  ls_F:显示文件类型
	目录文件：/
	普通文件：-
	链接文件：@
	其他文件：*
 ls_d：查看当前目录
 ls_ld：查看当前目录详细目录
 ls -l:显示详细列表：

                                                                           所有目录大小均为4096，操作系统不同，大小不同
    d           rwx        r-x        r-x        4        user    nobady   1024     Feb 22 09:20      a.txt
 文件类型   拥有者权限	同组人权限  其他人权限  链接号   拥有者   拥有组   大小   最后一次修改时间    文件
                                                该文件下所有子目录数，
						其包含当前目录和上一级目录
  
 拥有者-生成文件时登录的人,权限高, u表示        
 同组人-系统管理员分配的同组的一个或几个人, g表示         
 其它人-除拥有者,同组人外的人, o表示  
 所有人-包括拥有者、同组人及其它人, a表示  
  
  
  七大文件类型
directory：目录文件                       d
normal file：常规文件                     -
character device file:字符设备文件        c
block device file:块设备文件              b
socketfile：套接字文件                    s       
pipie inefile:管道文件                    p
linkfile:链接文件                         l

                           常规文件                       目录                  
r:  read              可以读取文件的内容               通过ls命令是否可以读取目录信息
w:  write          可以写入，修改和删除               创建子目录，修改文件名，复制文件
x:  execute执行权限   若是脚本，必须赋予脚本执行权限   是否可以通过cd 命令进入目录

                               常规文件                       目录
r:  read                可以读取文件的内容                通过ls命令是否可以读取目录信息
w:  write               可以写入，修改和删除              创建子目录，修改文件名，复制文件
x:  execute执行权限    若是脚本，必须赋予脚本执行权限     是否可以通过cd 命令进入目录


5.chmod 修改文件的权限   change modification
  命令格式：chmod mode filename 
   其中“mode”由三部分组成分别是：who、op、permission(s) 
    “who”表示用户或者组，又分为 u、g、o、a 
     “u” 表示文件所有者 
     “g” 表示文件所属组 
     “o” 表示文件除当前拥有者和所属组之外的其他用户以及组 
     “a” 表示上面所有的情况，即“a”表示文件的拥有者、所属组以及除当前拥有者和所属组之外的其他用户以及组
      “op” 表示修改用户对于文件权限的运算符，分为=、-、+ 
     “=” 表示设置权限，覆盖原来的权限   将原来的权限全部清空，再将赋予的权限加上去  
     “+” 表示当原来没有某个权限时，赋予改权限   +不会修改原来的权限,在原来的基础上增加赋予的权限
在原来的基础上增加赋予的权限
      “-” 表示移除某个已拥有的权限 
    “permission(s)” 表示用户或组对于文件的权限，分为r、w、x 
   例如：chmod g-r file1 
       chmod u+x,go+w file2 
       chmod a=rw file3
 
sudo chmod 777 /opt；以管理员的权限更改opt文件夹权限

八进制数来表示权限，命令格式：chmod 八进制权限 filename 
   八进制表示权限的解释：r=4，w=2，x=1，由此八进制0 到 7 的权限如下： 
   7：rwx；6：rw-；5：r-x；4：r--；3：–wx；2：-w-；1：--x；0：--- 
   例如：chmod 777 file1 
      chmod 655 file2 
6.touch 新建文件命令，当文件不存在是则创建文件，当文件已经存在时，则更新该文件的最后修改时间
为当前时间。 
7. mkdir 创建目录命令，创建一个目录，使用绝对目录或相对目录
命令格式：mkdir [-p] directory_name(s)   【-p】选项 可有可无
   一次可以创建多个目录 
   也可以一次创建一个多级目录 
    mkdir 3 4 5     单级目录
    mkdir a/b       多级目录，只会创建最后一级目录
-p:parent 父目录
检查副级目录是否存在，若不存在，先创建副级目录，再创建该目录

    当上级目录存在时，则直接创建相应的目录 
    当上级目录不存在时，则需要加“-p”选项 
8.cp 拷贝命令，既能拷贝文件也能拷贝目录 
    cp file1 file2 
    拷贝文件 file1 到 file1 所在的目录，并重命名为 file2 
   cp file1 dir1 
    拷贝文件 file1 到目录 dir1 中 
   cp -r file1 file2 dir1 dir2 
    当 cp 命令后面的参数大于两个的时候，最后一个参数必须是“目录” ，表示将所有参数(最后一
个目录除外)所表示的文件(或目录)拷贝到最后一个参数所表示的目录中 
    拷贝文件可以不加选项“-r” 
    拷贝目录必须加选项“-r” 
    cp -r[i]source_directory(s)destination_directory
 当源文件中存在目录，必须增加-i选项
-i info 提示信息   是否覆盖

9.mv 移动命令，既能移动文件，也能移动目录，跟cp 命令很相似,但是不用加“-r”选项。 
	eg.
		mv a.txt c/d:移动a文件到c/d下
		mv a.txt b.txt；将a重命名为b
		mv b.txt ~/c.txt:将b文件移动到家目录并重命名为c
		mv a c:将a目录下的所有东西移动到c目录下
		mv a b:a与b在同一目录下，为a重命名为b
10. rm 删除文件命令，只能删除文件，要删除目录需要加-r。 
11.rmdir 删除空目录，不需要加“-r”选项 
	eg.
		rm c.txt:删除c文件
		rmdir xxx:xxx为空目录，删除空目录
		rm -r xxx/:xxx为非空目录，删除目录
		sudo rm -r / :删除根目录下的所有东西
12. 压缩和解压缩 
常用的压缩格式有两种：gzip 和 bzip，在 bzip 的基础上出现了新的压缩格式bzip2 ,还有zip
  z代表  gzip 压缩时间短，但是压缩比低。压缩用gzip 命令， eg.gzip 1   压缩后：1.gz
  j代表  bzip2 压缩时间长，但是压缩比高 eg. bzip2 2   压缩后：2.bz2
   gzip 只能跟一个文件，跟多个文件名，则会分别压缩成多个文件，压缩文件后，把源文件删除，自动将原文件名称定为新名称，可以看出由什么压缩的
   bzip2   同上
   zip  filename.zip filename(s) 第一个参数表示压缩后的文件名，第二个参数表示要压缩的文件的文件名
   不会删除原文件，可以自己定义名称，俗成约定.后为zip
      eg.zip 89.zip 8 9   压缩后：89.zip
 解压缩：gunzip:解压缩文件后，把压缩文件删除    eg.3.gz  gunzip 3.gz  解压缩后：3
	 bunzip2:解压缩文件后，把压缩文件删除    eg.3.gz  gunzip 3.bz2  解压缩后：3
	 unzip:解压缩后不会删除压缩文件
	       eg.unzip 123.zip ps.123.zip包含多个文件  
	       解压缩后 若没有删除源文件，则解压缩后会询问是否覆盖
	       [y]es [n]o 是否对当前的文件替换/不替换[A]ll  [N]one 是否对所有的文件替换/不替换 [r]ename
                           
13.归档（打包）和解档 （解包）
   归档和解档都用的是 tar 命令，只不过跟的选项不一样。 
   归档：tar [zcvf] package_name file(s) 
   解档：tar [zxvf] package_name 
     -c  creat 打包
     -x extractn 解包
	这个选项可以不带-  eg. tar cf 123.tar 1 2 3 4
	带v （verbose 详细的）有打包文件的详细信息
	tar -cz    打包并用gzip压缩   
        tar -jz     打包并用bzip2压缩
    当解档时需要指定解档路径时使用“-C”选项，即 tar zxvf package_name -C destination_dir
    tar -zxvf+"空格""tab键"+回车：解压压缩包
13.ln 创建链接文件命令 
  链接又分为硬链接(Hard Link)和软链接(Soft Link) 
    硬链接 
	     硬链接相当于给文件起别名，当改硬链接文件修改时，对应的源文件内容也会随之更改。
	     建立硬链接可以防止误删 
	     硬链接不能跨分区 
	     一般对于目录不能建立硬链接 
	     eg.ln 1 1.hl
    软链接 
	     软链接相当于 Windows 文件系统中快捷方式。 
	     软链接也叫符号链接(Symbolic) 
	    命令格式：ln [-s] source_file destination_file 
	    说明：加选项“-s”为创建软链接，不加为创建硬链接 
	    eg. ln -s 1 1.sl
    
 △ man 查看其它命令的帮助文档 


元字符 



“Asterisk (*)” 星号，表示 0 到若干个任意字符 
	eg.ls d* 列出所有以d开头的文件
“Question Mask (?)” 问号，表示任意的单个字符 
	eg. ls d?  列出所有a?的文件 结果：da.txt
“Square Brackets ([])” 中括号，限制范围，表示在该元字符内的所有元素的任意一个示 b 到 f(包括 b、f)中的任意一个字符
	ls [d-j]b*    ps: d-j:出来的是dDeEfFhHiIjJ
	ls [fF]f*     表示fF中任意一个满足一个的文件
“Semicolon (;)” 分号，当多条命令在Shell中的一行时，用来分割每一条命令 
“Piping (|)” 管道，将两个命令分割开，会将第一个命令的结果作为第二个命令的输入传递给第二个
命令 
	eg.cal 7 2003 |lp
	   打开2003年7月的日历，链接打印机输出
“Angle Brackets (< > >>)” 重定向符号 
      <    重定向输入符号     标准输入来自文件，不来自键盘
      >    重定向输出符号     单个>覆盖
      >>   重定向输出符号     两个>追加



文本操作 

 cat 命令 
    命令格式：cat filename(s) 
    命令说明：将文件的内容显示到屏幕上 ，经常被用作文件的合并
              将文件内容进行重定向到其他文件或者设备中。例如：cat /etc/passwd > a.txt 
    扩展 
          eg.清空c.txt文件的内容
		cat /dev/null  >  ~/c.txt       /dev/null表示dev下的空文件
		: > c.txt
		cat c.txt > c.txt

         eg.cat /etc/passwd | awk -F: '{print $1 "\t" $6}' | sort > ~/userinfo 
         命令说明：将 passwd 的内容按照“:”进行分割，然后提取第一项和第六项交给 sort 命令，
sort 命令进行排序后重定向输出到userinfo 文件中。 
         空格和元字符作为分隔符时，在使用上述命令时，需要将空格或者元字符用单引号括起来。
 
   more 命令 
    命令格式：more filename(s) 
    命令说明：显示文件内容到屏幕上，但是一屏内容满了之后会暂停下来。此时可以通过其他功
能键进行查看。
      空格 向前
      b    向后
      f    向前
      q    退出
     less 和more功能一样但是less可以查找，而more不可以查找
	cat和more的异同
		同：将文件的内容显示到屏幕上
		异:cat ：将文件内容全部显示到屏幕上
		   more：分屏显示，需要自己操作显示信息



head 命令 
    命令格式：head [-n] filename(s) 
    命令说明：显示某个文件的前n 行，如果没有选项-n(n 是一个具体的数值)，则默认显示前10 行。 
tail命令 具有和 head 命令相类似的功能，不同的是从显示某个文件尾部的行。 
    eg.显示/etc/passwd 文件的倒数第五行到倒数第十行的内容。
        tail -10 /etc/passwd | head -5 
sort 命令 
    读取文件中的内容，按行排序。 (按照字典顺序排序)   eg.sort username 结果：1 10 2 5 8 9
    读取管道中的内容，按行排序。 
    可以加选项“-u”，表示重复行(相同并且相邻则视为重复行)只显示一次。 

uniq 命令 
    清除文件或者管道中的相邻的重复行 但是不排序
diff 命令 
    比较并显示两个文件的不同之处 ,及修改意见（针对第一个文件）
    命令格式：diff [-u] filename1 filename2 
        比较后：
		a:add 添加
		d:delete 删除
		c:change 改变
	eg.a.txt 1 2
	   b.txt 2 3
	   diff a b      结果：1d0  在d左边的数字代表第一个文件的行，右边的数代表第二个文件的行
			       <1 给第一个文件的第一行删除数据1
			       2a2 
			       >3
     另：如果给出的是先删除，后面操作晒在删除的基础上做；
         如果先给出的是增加，后面的删除实在增加之前做的
file 命令   file filename  eg. file a.tar.gz
    该命令用来查看文件类型 
echo 命令 
    拷贝在命令行中输入的，显示命令的内容到屏幕上 
    -n:取消换行符
	eg1. briup@student:~$ echo 123  结果 123   
	    briup@student:~$echo -n 123 结果 123briup@student:~$
        eg2. echo -e "1\n23"  结果 1   原因 n为元字符，在shell中有特殊含义，想让参数有意义，必须用双引号或单引号引起来
                                   23
     -e:让表达式生效
     常用于编写脚本
 script 命令   script a.txt
    录屏命令，该命令会执行之后会将控制台上所有从该命令之后的输入和输入全部显示到指定的
文件中。 
    录屏结束之后使用“exit”命令退出录屏。 

 

********  面试
8、全屏幕文本编辑器 
  Vi 编辑器（全屏幕文本编辑器，Full Screen Text Editor） 
   Vi 的三种工作模式  
	命令模式 command mode：可以执行一些简单命令，此模式是打开vi之后的默认模式
	编辑模式（插入模式） entry mode：可以编辑文本内容
	底行模式 last-line mode：可以执行一些比较复杂的命令的模式
     模式的切换模式：
        打开vi编辑器首先进入的是命令模式
	用户在命令模式下键入i/a/o 等命令之后，可进入插入模式。插入模式按esc回到编辑模式。
	    i 在光标当前位置进行插入。
	    a 在光标后面位置进行插入。
	    o 在光标所在位置的下一行新建一个空行进行插入。
        用户在命令模式下键入： / ? 等命令之后，可进入底行模式。插入模式按e回车回到编辑模式。

      vi的使用语法：
	> vi 创建一个临时文件，在此临时文件中编辑内容。在编辑完成之后需要及时进行保存，
	   在保存时需要提供文件名。
	> vi filename 如果filename不存在，则创建一个临时的文件来编辑，但是在保存的时
	候不需要提供文件名。如果filename存在，则直接打开该文件。
      保存文件：
       保存文件，在底行模式下输入  ：w
       保存并退出                  ：wq  或shift+zz或：x
       强制退出，退出但不保存      ：q!
       存成新文件                  :w 路径/newfilename
      操作：
        移动： 
	向上  k    
	向下  j    
	向左  h    
	向右  l （虽然还可以用，但现在直接按键盘的上下左右）
        ^（即 Shift+6）：移动到当前行的开头处 
        $（即 Shift+4）：移动到当前行的结尾处
	Ctrl+b：上滚一屏。 
        Ctrl+f：下滚一屏。 
        Ctrl+d：下滚半屏。 
        Ctrl+u: 上滚半屏。 
        删除：  
	（命令模式下操作）
		x 删除光标当前位置的一个字符  X 删除光标左边的一个字符 
		dw 删除当前词  （在vi编辑器中，从光标开始位置到下一个特殊标志为一个单词）
		3dw 删除三个词  eg. spy:5:das:89 删除后：：das:89
		dd 删除当前行  
		5dd 删除五行
	（底行模式下操作）
		:5,10d 删除第5行到第10行的内容
	        删除行的部分内容：将光标放置到该行要保存部分的右边，并输入D。为删除光标左边的所
有内容，须将光标放置到该行要删除部分的右边，并输入 d0（d-零）。 
                删除到文件的结尾：为删除从当前行到文件结尾的所有内容，需输入dG 
	替换：
		r 替换一个字符    
		cw 替换一个单词    
		cc 替换一行    
		C 替换从光标至行尾
	拷贝：
		yw    拷贝词 
		yy    拷贝行(或Y) 
		p   当前行下粘贴 
		:1,20co3  拷贝行1到行20在行3之后   co表示copy
		:3,5m6  移动行3到行5在行6之后    m表示mv
                                 
	 添加： 
	      输入 a 后，在光标的右边插入文本 
	      输入 A，在一行的结尾处添加文本 
	 插入： 
	      通过在命令模式下输入i，在光标的左边插入文本 
	      通过在命令模式下输入I，在行首插入文本 
	 插入新行： 
	      输入 o，在当前光标位置下面打开一行 
	      输入 O，在当前光标位置上面打开一行 
	 撤消更改 
	     撤消前一个命令：在最后一个命令之后立即输入u 来撤消该命令 
	     重复某个命令：“.” 
	     撤消对一行的更改：输入U 来撤消你对一行所做的所有更改，这个命令只有在你没将光标移
	动到该行以外时才生效。 

	 查找一个字符串 ？ /
	     /  从所查找的位置开始往下搜索
	     ： 从所查找的位置开始往上搜索
	     输入“n”跳转到该串的下一个出现处 
	     输入“N”跳转到该串的上一个出现处 
	 替换一个字符串 
	     在一行内替换头一个字符串 old 为新的字符串new：:s/old/new 
	     在一行内替换所有的字符串 old 为新的字符串new：:s/old/new/g 
	     在两行内替换所有的字符串 old 为新的字符串new：:#,#s/old/new/g 
	     在文件内替换所有的字符串 old 为新的字符串new：:%s/old/new/g 
	     进行全文替换时询问用户确认每个替换需添加c 选项：:%s/old/new/gc（需按两次回车） 
	  设置 vi 
	     显示行号:set number 
	     取消行号显示:set nonumber 
	     设置显示用户模式:set showmode 
	     设置文件只读:set readonly 
vi 高级设置
	~  改变大小写 
	J  把当前行和下一行连起来 
	u  废除刚才的编辑操作(undo) （撤销） ctrl+r 恢复刚才的撤销
	:set nu 显示行代码 (:set nonu) 即显示行号
	:21 光标停在指定行 
	21G 光标停在指定行 (G 到文件尾,1G 到文件头) 命令模式下
	/串 从前往后找 底行
	?串 从后往前找 底行
	n  查找继续 
	:r file2 在光标所在位置插入另一个文件 ，在下一行插入文件
	:1（第一行）,$（最后一行）s（替换）/旧串/新串/g 替换全文(或 :%（全文）s/旧串/新串/g) 

	不加g全部替换，加g只替换每一行中的第一个

day03

在 Linux 系统中查找文件
自动搜索：path环境变量相关
手动搜索
      find：搜索文件
             find path  expression [action]
	     find ~ -name "1*.txt" 在家目录下按名字搜索以txt结尾的文件
	     find 命令自身就会进行递归搜索
			find /a/b
	   find 命令选项
		-perm 按照文件权限来查找文件。 
			eg. find ~ -perm 777 > ~/holes 找出家目录下可能存在漏洞（权限为777）的文件路径，并输出到holes(漏洞文件)文件中
		-type 查找某一类型的文件，诸如： 
			     b - 块设备文件。 
			     d - 目录。 
			     c - 字符设备文件。 
			     p - 管道文件。 
			     l - 符号链接文件。 
			     f - 普通文件。 
                     eg.find /export/home -type f -atime +365 -exec rm {} \; 按照文件类型在家目录下查找
                 -prune 使用这一选项可以使 find 命令不在当前指定的目录中查找，如果同时使用-depth 选项，那么-prune 将被 find 命令忽略。 
		 -user 按照文件属主来查找文件。 
		 -group 按照文件所属的组来查找文件。 
		 -name 按照文件名查找文件。
		 -mtime -n +n  按照最后一次修改时间搜索
			-n 表示文件更改时间距现在 n 天以内，+n表示文件更改时间距现在n 天以前。
			eg. find ~ -mtime +10 最后一次修改距离现在10天以前
			    find ~ -mtime -10 最后一次修改距离现在10天以内
		 -atime   按照最后一次访问时间搜索
		 -user 后面可以跟用户名，跟UID
			find/etc -user 0  //root用户的ID为0
			find /etc -user briup 查找文件属于用户briup的文件
		 -ctime 查找系统中最后 n*24 小时被改变文件状态的文件 

		 [ation]
		 -print:默认action,
		 -exec:  1）将容器中的文件名挨个取出，和  -exec后面的所有参数拼接成一个命令
			 2）执行拼接命令
			eg. find . -name "*.txt" -exec rm -r {} \ ;
			删除满足条件的文件
			find . -name "*.txt" 显示满足条件的文件
			{} 拼接命令成 rm -r 4.txt rm -r 5.txt rm -r 6.txt
			\  
			;  在拼接后的命令中间加入；形成三个命令	
		 -ok    eg. find . -name "*.txt" -ok rm -r {} \ ;
			会提示是否删除
		
		
    grep:搜索字符串
	grep 字符串 文件
	grep briup /etc/passwd(加-i忽略大小写)
                           加-v列出所有不匹配要求的文件
    wc （word count ）词频统计 eg.wc /etc/passwd  
                                  结果 40(行数) 69(单词) 2240(字符) /ect/passwd
			 -l 统计行 
			 -w 统计单词 
			 -c 统计字符数 
   df    查看磁盘当前使用情况	df [选项] [文件]   
	     -a 全部文件系统列表 
	     -h 方便阅读方式显示 
	     -H 等于“-h”，但是计算式，1K=1000，而不是 1K=1024 
	     -i 显示 inode 信息 
	     -k 区块为 1024 字节 
	     -l 只显示本地文件系统 
	     -m 区块为 1048576 字节 
	     --no-sync 忽略 sync 命令 
	     -P 输出格式为 POSIX 
	     --sync 在取得磁盘信息前，先执行 sync 命令 
	     -T 文件系统类型 

id 命令 
	不跟用户名表示用来查看当前用户的用户信息（UID，用户名，GID，组名，所属所有组）
	跟用户名表示查询该用户的UID和GID

who 查看当前用户信息
	1 用户名 2 终端 3 时间 4（）内IP地址
 ps 命令 
    命令说明：用于显示当前进程 (process) 的状态 
    命令语法：ps [options] [--help] 
    参数说明： 
     -e 查看当前系统所有用户启动的进程
     -f 列出每一个进程所执行的命令
     -U 列出每一个进程所执行的命令

  kill命令 
    命令说明：用于删除执行中的程序或工作。kill 可将指定的信息送至程序。
    命令语法：kill [-s <信息名称或编号>][程序]或 kill [-l <信息编号>] 
    kill -9 pid 杀死自己所开启的进程   pid 进程编号，通过ps -ef | grep -i name查询（第二项）
                                       -9 给进程发送一个信号量，即请进程立马结束
    sudo kill -9 840 杀死其他用户进程

 job control 工作控制
     sleep 5 睡眠5s
     结束睡眠 ctrl+c   ctrl+z 停止前台工作
     唤醒已经停止的作业 : fg %n   bg %n   n为具体的作业编号 可用jobs查看作业编号（【】内为作业编号）
     杀死指定作业 kill %n

ifconfig； 查看网络信息和设置网络信息。
	设置网络信息：sudo ifconfig ens32 1.0.0.222 netmask 255.255.255.0 gateway 1.0.0.254
	              ens32 网卡名字 1.0.0.222 自己想要设置的网卡名字 
		      netmask 255.255.255.0 子网掩码    gateway 1.0.0.254 网关
ping: 测试到目标主机的网络是否连通。 eg.ping www.baidu.com 测试到百度的网址是否连同

write 给当前登录到操作系统当中的所有用户发送消息

day 04

who 查看当前登录用户的信息
	登录终端：tty  在本地登录
	          pts   远程登录    row 行   column列  narrow 窄
w 查看服务器上所有在线用户正在执行的操作

whoami  查看当前登录的客户名
who am i  查看远程登录的客户名


ftp: file transport protocol  文件传输协议
HTTP:hyper text transport protocol 超文本传输协议

scp 远程拷贝命令
scp 源文件 目的文件
	eg. 服务器有一文件 文件 ~/1.txt,拷贝到本地
	   scp  -r boss@172.16.7.250:~/1.txt ~
	           远程主机文件

	  本地文件2.txt 拷贝到远程服务器
	  scp -r ~/2.txt boss@172.16.7.250:~
	  -r 有目录时使用

cp 源文件 目的文件
	只能在本地拷贝






脚本编程  以sh 结尾


 模拟回收站功能：
 回收站原理：将文件暂时存放在一个目录中，如果真的不需要这些文件的话，清楚回收站。
1.创建一个目录
2.获取要删除的文件
3.将想要删除的文件移动到该目录
4.移动文件时进行重命名

脚本程序：

#!/bin/bash

# Make a directory named ".trash"
TRASH_DIR="home/kevin/.trash"   //定义目录的路径
mkdir -p ${TRASH_DIR}    //创建目录

FILES=${@}    //接收删除的全部文件，以参数的形式传给FILES
for FILE in ${FILES}     //移动FILES里的每一个文件
do                     //将文件移动到TRASH_DIR里，进行重命名
	TIME=`date +%Y%m%d%H%M%S`
	mv ${FLIE} ${TRASH_DIR}/${FILE}.${TIME}
done




# 表示注释    进入脚本第一行带有#，表示告诉操作系统用哪种shell来执行脚本
		eg.#！/bin/bash 告诉脚本使用哪种shell
${0} 代表命令本身  ${1} 代表参数  ${@} 代表全部参数

TRASH_DIR：变量 脚本中变量全部大写

mkdir -p ${TRASH_DIR} 创建目录




tr [A-Z] 1 把大写的A-Z变为1
*每一分钟，即任意时刻
crontab 计划任务
sudo vi /etc/crontab
一行代表一个计划任务
#    m    h      dom      mon  dow     user    command
   分钟  小时    几号   月份   周几    用户    执行的事

crond 服务
/etc/crontab 文件是crond的配置文件

0 2 * * * /tmp/a.txt   每天两点执行这个命令
0,30 8-17 * * 1-5 /tmp/c.txt  整点和半点，8点到17点，周一到周五执行


设置初始化文件
	环境变量配置文件
		系统环境变量文件：/etc/profile   多用户系统，更改系统环境变量，所有用户都可以使用
		用户环境变量文件：~/.bashrc 只针对当前用户


在Linux下解压缩文件

sudo mkdir -p /opt/softwares /opt/links      //opt目录下创建两个目录
sudo chown briup :briup /opt/*               //更改某个用户的所属者和所属组
tar -zxvf jdk-8u181-linux-x64.tar.gz -C /opt/softwares   //-C指定解压到某位置
ln -s /opt/softwares/jdk1.8.0_181 /opt/links/jdk    //创建软连接，使/opt/softwares/jdk1.8.0_181指向/opt/links/jdk
							/opt/links/jdk是/opt/softwares/jdk1.8.0_181的软连接
配置环境变量：
>vi ~/.bashrc

export JAVA_HOME=/opt/links/jdk   //配置一个环境变量为AVA_HOME
export JRE_HOME=$JAVA_HOME/jre
export CLASSPATH=.:$JAVA_HOME/lib:$JRE_HOME/lib  
export PATH=$PATH:$JAVA_HOME/bin

source ~/.bashrc   //让配置的环境变量生效


day 05

which 用于查找文件。which 指令会在环境变量$PATH 设置的目录里查找符合条件的文件。
 
whereis 用于查找文件。该指令会在特定目录中查找符合条件的文件。

history 显示历史记录   ！106 执行历史记录中的106行的历史命令 ！！刚才所执行的历史记录

aliases  起别名 对当前启动的终端有效
           alias sls = "ls"
 
 export PATH = 1 配置临时环境变量  生效：配置临时变量的shell中
				    失效：关闭终端

 用户管理

 useradd:添加用户
	sudo useradd -m -d /home/xyz -s /bin/bash xyz_
	-u 指定UID  -g 指定所属组 -m 创建家目录 -d 创建家目录下的xyz -s 指定使用的shell
	没有指定UID则会默认当前用户所有用户最大的UID+1
	没有组会默认创建相同名字的组

 userdel:删除用户
         sudo userdel xyz1 删除用户，但不删除家目录
	 -r 删除家目录
 usermod:修改用户
	-l 修改新的用户名
	-d 修改家目录

查 sudo cat /etc/paawd /etc/group |grep -i UserName

用户组管理

groupadd
groupdel
groupmod
创建组 
参数：-g GID :指定GID的值 
例： groupadd g1 ?
删除组 
格式：groupdel 组名 
例： groupdel g1 ?
修改组信息 
格式：groupmod [参数] 组名 
参数：-n 新组名：修改组的名字 
-g GID：修改组的GID 
例： groupmod -n group1 g1      g
     roupmod -g 860 g1 ?
 添加／删除组成员 
 格式：gpasswd [参数] 组名 
 参数：-a 用户名 ： 向指定组添加用户 
 -d 用户名 ： 从指定组中删除用户 
 例： gpasswd -a u1 root      
      gpasswd -d u1 root ?
 查看用户所属组 
 格式：groups [用户名]


文件所属

chmod 修改用户对于文件的权限
chown 修改文件的所属者和所属组
	普通用户无权执行，用root用户执行
	sudo chown root 1(用户名) 只修改文件的所属者
	sudo chown :root 1 只修改文件的所属组
	sudo chown root :root2 既能修改文件的所属者，又能修改所属组
chgrp 修改文件的所属组




*********************************************************************************

掌握：

操作系统软件的组成：kernel 内核，管理硬件
		    shell 用户和内核之间的桥梁
		    fileSystem文件系统，存储数据

Pathname 路径名，能够确定一个文件位置的路径名称。
	一个路径名只能唯一确定一个文件
	一个文件可以有多个路径。

绝对路径 路径名相对于根目录
相对路径路径名相对于当前工作目录

远程登录
	telnet telnet ip
	ssh 
		ssh username@ip  以username的身份登录
		ssh ip 以执行这条命令的身份登录
登出： exit logout

/etc
	|--passwd 储存用户信息，但不包括密码
	|--shadow  储存用户的密码信息，只有root用户具有读写权限
	|--group  储存用户组信息

基本的Linux命令
clear
pwd
cd: *
ls: *
	ls -l显示文件的长列表信息。主要关注权限信息和文件所属者、组信息。

sudo chmod 修改权限信息
	权限模式sudo chmod u+xg-w,o=r,a=rwx filename
	权限值sudo chmod 777 filename
cp
mv
rm
	cp和rm在操作的时候最好直接加上-r选项
	mv一定不能加-r

ln 
	软连接 ln -s s d
	硬链接 ln s d
	命令不能创建硬链接

gzip  gunzip
bzip2   bunzip2  
	压缩好删除源文件，并且压缩包的命名是源文件的名字.gz或.bz2后缀
zip unzip
	和上面命令相反

tar 
	打包 tar -[z/j]cvf pn file(s)
	解包 tar -[z/j]xvf pn [-C 解压之后要存放的文件路径】

元字符
	* ？ 【】 | ； < > >>
cat 显示文件的全部内容
more/less 只显示一个屏幕，需要使用空格等按键控制
head/tail 查看文件头尾的内容
sort 对文件内容进行字典顺序排查
uniq 去重
diff 比较两个文件的不同之处，修改意见是针对该命令后第一个文件
file 查看文件类型 
echo 输出该命令后面的内容至屏幕




！！！***vi
 
 命令模式 默认
 编辑模式 a i o A I O
 底行模式: ? /


find 查找文件
grep 查找字符串

ps 查看操作系统中的进程
	ps -ef
sudo kill -9 pid

ifconfig 查看和设置网络信息
ping 测试到目标主机的网络是否连通

scp 基于ssh的远程拷贝命令
cron crontab 
初始化文件 配置环境变量
	/etc/profile
	~/.bashrc

用户管理和用户组管理
	对于用户和用户组的增删改查
文件管理 更改文件的所属者和所属组
	chown
	chgrp
	

安装软件包
sudo apt install firefox


















